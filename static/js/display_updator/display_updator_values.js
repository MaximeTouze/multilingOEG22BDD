// Display updator memory values

// The last display
let previous_display = WORD_CLOUD;

// boolean for knowing if an update is currently ungoing
let updateUngoing = false;

// the current nb of displayed sentences
let current_nb_sentence = 0;
